const toggle=document.querySelector('.theme-toggle');const savedTheme=localStorage.getItem('theme');if(savedTheme==='dark')document.body.classList.add('dark');if(toggle)toggle.addEventListener('click',()=>{document.body.classList.toggle('dark');localStorage.setItem('theme',document.body.classList.contains('dark')?'dark':'light')});
(()=>{const c=document.getElementById('physics-field');if(!c||matchMedia('(prefers-reduced-motion: reduce)').matches)return;const x=c.getContext('2d'),m={x:innerWidth/2,y:innerHeight/2},C=['#46e6ff','#ff4fa3','#8d6bff','#c6ff4a'];let p=[],b=[],t=0;function resize(){const d=Math.min(devicePixelRatio||1,2);c.width=innerWidth*d;c.height=innerHeight*d;c.style.width=innerWidth+'px';c.style.height=innerHeight+'px';x.setTransform(d,0,0,d,0,0);p=Array.from({length:Math.min(180,Math.max(70,Math.floor(innerWidth*innerHeight/10000)))},(_,i)=>({a:Math.random()*6.28,r:60+Math.random()*Math.min(innerWidth,innerHeight)*.62,s:(.0008+Math.random()*.002)*(Math.random()<.5?-1:1),w:Math.random()*10,z:.4+Math.random()*1.5,q:Math.random()*6.28,col:C[i%4]}))}addEventListener('resize',resize,{passive:true});addEventListener('pointermove',e=>{m.x=e.clientX;m.y=e.clientY},{passive:true});addEventListener('pointerdown',e=>b.push({x:e.clientX,y:e.clientY,r:0,a:1}),{passive:true});resize();function frame(){t++;x.clearRect(0,0,innerWidth,innerHeight);const cx=innerWidth*.5,cy=innerHeight*.48,pts=p.map(o=>({x:cx+Math.cos(o.a+t*o.s)*o.r+Math.sin(t*.001+o.q)*o.w,y:cy+Math.sin(o.a+t*o.s)*o.r*.57+Math.cos(t*.0012+o.q)*o.w}));for(let i=0;i<pts.length;i++)for(let j=i+1;j<pts.length;j++){const d=Math.hypot(pts[i].x-pts[j].x,pts[i].y-pts[j].y);if(d<90){x.beginPath();x.moveTo(pts[i].x,pts[i].y);x.lineTo(pts[j].x,pts[j].y);x.strokeStyle=`rgba(70,230,255,${(1-d/90)*.11})`;x.lineWidth=.55;x.stroke()}}pts.forEach((q,i)=>{const d=Math.hypot(m.x-q.x,m.y-q.y);if(d<220){p[i].a+=(1-d/220)*.018;p[i].r=Math.max(35,p[i].r-(1-d/220)*.22)}x.beginPath();x.arc(q.x,q.y,p[i].z,0,6.28);x.fillStyle=p[i].col;x.globalAlpha=.5;x.fill();x.globalAlpha=1});for(let k=0;k<4;k++){const r=80+((t*.35+k*145)%560);x.beginPath();x.arc(cx,cy,r,0,6.28);x.strokeStyle=`rgba(255,79,163,${.075*(1-r/620)})`;x.lineWidth=.7;x.stroke()}b=b.filter(o=>o.a>.015);b.forEach(o=>{o.r+=9;o.a*=.93;x.beginPath();x.arc(o.x,o.y,o.r,0,6.28);x.strokeStyle=`rgba(70,230,255,${o.a*.4})`;x.lineWidth=1.3;x.stroke()});const g=x.createRadialGradient(cx,cy,0,cx,cy,260);g.addColorStop(0,'rgba(255,79,163,.1)');g.addColorStop(.4,'rgba(70,230,255,.03)');g.addColorStop(1,'rgba(0,0,0,0)');x.fillStyle=g;x.beginPath();x.arc(cx,cy,260,0,6.28);x.fill();requestAnimationFrame(frame)}frame()})();